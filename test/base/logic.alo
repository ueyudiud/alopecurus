let t = true
let f = false

-- true test
assert(t)

-- false test
assert(!f)

-- logical or
assert(!(f || f))
assert(f || t)
assert(t || f)
assert(t || t)

-- logical and
assert(!(f && f))
assert(!(t && f))
assert(!(f && t))
assert(t && t)

-- ternary predicate
assert(t ? t : f)
assert(f ? f : t)
